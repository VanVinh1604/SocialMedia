<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">

    <!-- Viền xám đậm (giống Instagram chính chủ) -->
    <stroke
        android:width="2.5dp"
        android:color="#DBDBDB" />

    <!-- Nền trong suốt -->
    <solid android:color="@android:color/transparent" />

    <size
        android:width="72dp"
        android:height="72dp" />
</shape>